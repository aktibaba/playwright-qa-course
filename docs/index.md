---
layout: home

hero:
  name: "Playwright + TypeScript"
  text: "QA Automation Course"
  tagline: Build a production-grade API + UI automation framework against a real, dockerized app.
  actions:
    - theme: brand
      text: Start with Chapter 1
      link: /chapters/01-why-a-framework
    - theme: alt
      text: All chapters
      link: /chapters/

features:
  - title: Real system under test
    details: A dockerized React + Express + Postgres app (Inkwell) with deterministic reset/seed endpoints — tests never race startup or fight flaky data.
  - title: Layered architecture
    details: utils → fixtures → pages → tests. Custom fixtures, Page Objects as fixtures, and a single @fixtures import.
  - title: API + UI + Integration
    details: Test a documented API contract, drive the UI, then combine them — seed via API, verify in the UI.
---
